spring:
  application:
    name: players
  datasource:
    url: jdbc:mysql://localhost:3306/playersdb?useSSL=false&serverTimezone=Asia/Jerusalem
    username: root
    password: lolo
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
csv:
  file-name: "player.csv"